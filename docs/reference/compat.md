# Runtime compatibility

Bytefold targets:

- Node 24+
- Latest stable Deno
- Latest stable Bun
- Web (browser) entrypoint with HTTPS-only URL inputs

The support matrix in `SPEC.md` is the source of truth for format and runtime
coverage.
